# from django.urls import path

# from .views import SignupPageView

# urlpatterns = [
#     path("signup/", SignupPageView.as_view(), name="signup"),
# ]

# They were commented out because they are no longer needed after
# django's allauth package
